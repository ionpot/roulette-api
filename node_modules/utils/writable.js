"use strict";

var S = require("stream");

exports.create = function () {
	var w = new S.Writable({
		decodeStrings: false,
		write: function (chunk, enc, done) {
			this.out += chunk;

			done();
		}
	});

	w.out = "";

	return w;
};
